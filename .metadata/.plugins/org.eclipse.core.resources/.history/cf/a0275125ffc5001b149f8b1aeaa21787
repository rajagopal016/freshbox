import java.io.*;
public class Stream {
	void streamf() throws IOException {
		Variables v2 = new Variables();
		FileOutputStream fout;
		fout = new FileOutputStream("f.txt");
		ObjectOutputStream out = new ObjectOutputStream(fout);
		out.writeObject(v2);
		out.flush();
		System.out.println("success");
		
	}

}
